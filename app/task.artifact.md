# Task List: BLE Connection Implementation (Phase 1)

- `[ ]` Update `AndroidManifest.xml` with Bluetooth and Location permissions
- `[ ]` Implement `BluetoothManager.kt` for BLE scanning and connection
- `[ ]` Implement `BleScannerViewModel.kt` to manage UI state
- `[ ]` Update `MainActivity.kt` with scan/connect UI and permission handling logic
