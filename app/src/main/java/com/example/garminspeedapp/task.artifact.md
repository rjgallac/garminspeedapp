# Tasks

- `[ ]` Fix UI logic in `MainActivity.kt` to enable the "Start Scanning" button by default
- `[ ]` Implement runtime permission handling for Bluetooth and Location permissions in `MainActivity.kt`
- `[ ]` Add check/prompt to handle Bluetooth being disabled
